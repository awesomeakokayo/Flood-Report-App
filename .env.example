# PostgreSQL connection string (Neon/RDS/any Postgres)
DATABASE_URL = 'postgresql://user:password@host:5432/dbname?sslmode=require'

# Google Gemini API key for AI media verification
GEMINI_API_KEY = ''

# Optional: Firebase service account JSON (single-line).
# The app also reads app/firebase_service.json if it exists.
FIREBASE_SERVICE_ACCOUNT = ''
